services:
  poker:
    image: ghcr.io/svoronkin/scrum-poker:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - poker-data:/app/data
    restart: unless-stopped

volumes:
  poker-data:
